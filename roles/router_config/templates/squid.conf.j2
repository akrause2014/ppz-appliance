acl localnet src 0.0.0.1-0.255.255.255  # RFC 1122 "this" network (LAN)
acl localnet src 10.0.0.0/8             # RFC 1918 local private network (LAN)
acl localnet src 169.254.0.0/16         # RFC 3927 link-local (directly plugged) machines
acl SSL_ports port 443
acl Safe_ports port 80          # http
acl Safe_ports port 443         # https

acl goto_github dstdomain .github.com
http_access allow goto_github

acl goto_ubuntu dstdomain .ubuntu.com
http_access allow goto_ubuntu

acl goto_canonical dstdomain .canonical.com
http_access allow goto_canonical

acl goto_ed dstdomain .ed.ac.uk
http_access allow goto_ed

acl allow_ppz dstdomain "/etc/squid/allowlist_domains.txt"
http_access allow allow_ppz

acl allow_ppz2 dstdomain "/etc/squid/allowlist_urls.txt"
http_access allow allow_ppz2

acl allow_ppz3 dstdomain api.snapcraft.io
http_access allow allow_ppz3


http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports
http_access allow localhost manager
http_access deny manager
include /etc/squid/conf.d/*.conf
http_access allow localhost
http_access deny all

http_port 3128
http_port 3129 ssl-bump cert=/etc/squid/cert/squid_proxyCA.pem generate-host-certificates=on dynamic_cert_mem_cache_size=4MB
sslcrtd_program /usr/lib/squid/security_file_certgen -s /var/spool/squid/ssl_db -M 4MB
acl step1 at_step SslBump1
ssl_bump peek step1
ssl_bump bump all


coredump_dir /var/spool/squid
refresh_pattern ^ftp:           1440    20%     10080
refresh_pattern ^gopher:        1440    0%      1440
refresh_pattern -i (/cgi-bin/|\?) 0     0%      0
refresh_pattern \/(Packages|Sources)(|\.bz2|\.gz|\.xz)$ 0 0% 0 refresh-ims
refresh_pattern \/Release(|\.gpg)$ 0 0% 0 refresh-ims
refresh_pattern \/InRelease$ 0 0% 0 refresh-ims
refresh_pattern \/(Translation-.*)(|\.bz2|\.gz|\.xz)$ 0 0% 0 refresh-ims
refresh_pattern .               0       20%     4320
