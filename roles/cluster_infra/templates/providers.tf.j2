# Define required providers
terraform {
required_version = ">= 0.14.0"

  # need the openstack provider
  required_providers {
    openstack = {
      source  = "terraform-provider-openstack/openstack"
    }
  }
}
